---
- name: Bootstrap Ansible and OpenTofu configuration
  hosts: localhost
  become: true
  connection: local
  gather_facts: false
  roles:
    - controller

  tasks:
    - name: Install bw CLI
      ansible.builtin.import_role:
        name: secrets
        tasks_from: install_client
