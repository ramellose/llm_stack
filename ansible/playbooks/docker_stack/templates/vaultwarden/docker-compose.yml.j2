services:
  vaultwarden:
    image: vaultwarden/server:{{ service_config[item.service].version }}
    container_name: vaultwarden
    restart: unless-stopped
    environment:
      DOMAIN: "https://vaultwarden.{{ base_domain }}"
    volumes:
      - {{ compose_base_path }}/{{ item.service }}/data/:/data/
    ports:
      - 8000:80
