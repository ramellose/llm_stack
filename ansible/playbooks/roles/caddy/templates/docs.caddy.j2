# {{ svc.service }} — static site
{{ svc.public_hostname }} {
    root * {{ cfg.root | default('/srv/' ~ svc.service) }}
    file_server
    tls {
        dns cloudflare {$CLOUDFLARE_API_TOKEN}
                resolvers 1.1.1.1
    }
}
